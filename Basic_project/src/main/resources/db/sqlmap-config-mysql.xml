<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMapConfig PUBLIC "-//ibatis.apache.org//DTD SQL Map Config 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-config-2.dtd" >

<!-- 
	iBATIS 연동과 관련하여 Query를 담고 있는 map 파일을 관리합니다.
	iBATIS config는 settings의 일부Attribute value가 true/false등으로만 넣도록 되어 있어서 properties로 분리는 일부 항목만 가능.
	
	cacheModelsEnabled : SqlMapClient를 위한 모든 캐쉬모델의 가능여부 설정. 디버깅시 도움.
	 - default = true
	enhancementEnabled : 향상된 lazyLoading처럼 최적화된  자바빈즈 속성 접근을 위해 런타임시 바이트코드 향상을 가능하게 한다.
	 - default = true
	lazyLoadingEnabled : SqlMapClient를 위한 모든 늦은(lazy)로딩의 가능여부 설정. 디버깅시 도움.
	 - default = true
	maxRequests : 한번에 SQL문을 수행할 수 있는 쓰레드의 갯수 설정. 이보다 많은 요청은 다른 쓰레드가 수행완료시점까지 블록.
	 - 보통 최소 maxTransactions의 10배.
	 - 항상maxSessions, maxTransactions보다 크다.
	 - 종종 최대치를 줄이면 성능향상.
	 - default = 512
	maxSessions : 주어진 시간동안 활성화될 수 있는 세션갯수 설정.
	 - 명시적, 프로그램적, SqlMapClient를 사용할때마다 자동적으로 생성될 수 있다.
	 - 항상 maxTransactions보다 같거나 커야하고, maxRequests보다 작아야 한다.
	 - 최대치를 줄이면 전체적인 메모리 사용량을 줄일 수 있다.
	 - default = 128
	maxTransactions : 한번에 SqlMapClient.startTransaction()에 들어갈 수 있는 쓰레드 최대 갯수 설정.
	 - 설정보다 많은 쓰레드는 다른 쓰레드가 완료될때까지 블록.
	 - maxSessions보다 작거나 같아야 하고, maxRequests보다 작아야 한다.
	 - 종종 최대치를 줄이면 성능향상.
	 - default = 32
	defaultStatementTimeout : 모든 구문에 대한 JDBC 쿼리 타임아웃 초단위의 정수값 설정.
	 - 매핑구문의 'statement' 속성으로 무시된다. defaultStatementTimeout과 statement이 설정되지 않으면 쿼리타임아웃이 세팅되지 않는다.
	 - 모든 드라이버가 이 설정을 지원하지는 않으므로 확인 후 사용할것.
	classInfoCacheEnabled : classInfoCacheEnabled 설정을 사용하도록 설정하면 iBATIS는 클래스의 캐시를 관리하며(재사용) 많은 클래스가 재빨리 시작하도록 지원.
	 - default = true
	useStatementNamespaces : sqlmap 이름 + statement이름으로 구성된 fullQualifiedName으로 매핑 구문을 참조하도록 하게 한다.
	 - default = false
	statementCachingEnabled : 사용시 iBATIS는 prepared 구문의 지역적인 캐시를 관리한다. 명확한 성능향상을 보여준다.
	 - default = true
-->

<sqlMapConfig>
	<settings
		cacheModelsEnabled="true"
		enhancementEnabled="true"
		lazyLoadingEnabled="true"
		maxRequests="128"
		maxSessions="32"
		maxTransactions="8"
		defaultStatementTimeout="10"
		useStatementNamespaces="true"
	/>
	
	<sqlMap resource="com/whereami/web/sqlmap/WhereamiSQLMap.xml"/>
	
</sqlMapConfig>
